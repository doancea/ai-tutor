# Required for the onboarding interview's plan-generation call.
# Get one from https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional. Set to "true" to seed a fresh install with the original
# hand-authored CCAR-F demo plan instead of starting with the onboarding
# interview — useful for developing on the tracker UI without an API key.
SEED_DEMO=
